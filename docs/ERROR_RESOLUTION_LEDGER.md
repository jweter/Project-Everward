# Error Resolution Ledger

This ledger records verified development and CI failures whose root causes are useful to preserve as project knowledge. Entries should be concise, evidence-based, and focused on preventing recurrence.

## 2026-08-17 — Rendering benchmark test import failed on Python 3.12

- **Observed failure:** `Foundation checks` failed while importing `prototypes/rendering-benchmark/test_benchmark.py`; all earlier Phase 1 prototype suites passed.
- **Error:** `AttributeError: 'NoneType' object has no attribute '__dict__'` inside `dataclasses._is_type` while applying `@dataclass` in `benchmark.py`.
- **Root cause:** the test loaded `benchmark.py` with `importlib.util.module_from_spec()` and executed it directly without first registering the module in `sys.modules`. Python 3.12 `dataclasses` resolves annotation/module context through `sys.modules[cls.__module__]`, so the nonstandard loader left that lookup missing.
- **Fix:** register the dynamically created module in `sys.modules` before `spec.loader.exec_module(benchmark)`.
- **Scope:** test-loader correction only; no benchmark scoring or production simulation behavior changed.
- **Prevention:** when tests dynamically load modules that define dataclasses or other module-introspective constructs, emulate normal import semantics by inserting the module into `sys.modules` before execution, or prefer a normal package import where practical.
- **Verification:** fresh GitHub Actions run required on the updated PR head.

## 2026-08-17 — Rendering benchmark pipeline tests drifted from canonical scenario identity

- **Observed failure:** PR #13 `Foundation checks` passed foundation, simulation-clock, procedural-system, coordinate-scale, and headless-simulation suites, then failed two rendering benchmark pipeline tests.
- **Error:** `ValueError: run record scenario_name does not match scenario` in `run_record.validate_run_record()`.
- **Root cause:** the new pipeline test fixture hard-coded `scenario_name` as `icy_asteroid_mining_v1`, while the authoritative `scenario.json` contract names the scenario `icy-asteroid-mining`. The validator correctly rejected the stale test fixture.
- **Fix:** derive both `scenario_version` and `scenario_name` in pipeline fixtures directly from the canonical `scenario.json`, and assert against that same authoritative identity instead of duplicating literals.
- **Scope:** test-fixture correction only; validation, normalization, scoring, and production simulation behavior remain unchanged.
- **Prevention:** tests for versioned/canonical contracts should load identity fields from the authoritative fixture unless the purpose of the test is explicitly to verify mismatch rejection. Avoid duplicating contract identifiers across test helpers.
- **Verification:** fresh GitHub Actions run required on the updated PR head.

## 2026-08-17 — Rendering scene manifest lagged canonical scenario version

- **Observed failure:** PR #17 `Foundation checks` passed every prior Phase 1 suite and 46 rendering-benchmark tests, then failed all six new `test_scene_state.py` cases.
- **Error:** `ValueError: scene manifest scenario_version does not match scenario` from `scene_state.validate_manifest()`.
- **Root cause:** the canonical rendering scenario had already advanced to `scenario_version: 2` when deterministic playback timing was added, but the newly introduced `scene_manifest.json` was authored with stale `scenario_version: 1`. The validator correctly treated the manifest as incompatible rather than silently accepting drift.
- **Fix:** bind `scene_manifest.json` to canonical scenario version 2. No scene geometry, animation timing, playback logic, or simulation behavior changed.
- **Scope:** benchmark metadata correction only; renderer-neutral scene truth remains unchanged.
- **Prevention:** every new persisted artifact that binds to a versioned canonical contract must copy its identity from the current authoritative contract at creation time and include a CI test that validates the binding before any deeper behavior assertions. Treat version mismatch as a hard failure, never an implicit migration.
- **Verification:** fresh GitHub Actions run required on the updated PR head.

## 2026-08-18 — Capture readiness audit referenced stale Unreal source filenames

- **Observed failure risk:** the Phase 1 hardware-capture readiness contract required `BenchmarkCaptureSession.h/.cpp`, but the merged Unreal implementation uses `BenchmarkCaptureSessionComponent.h/.cpp`. The synthetic test fixture created whatever paths the contract requested, so CI could pass while the audit remained inconsistent with the real repository layout.
- **Root cause:** the readiness file list was authored against an earlier Unreal capture-session name and the fixture tests derived their filesystem entirely from that same list, creating a self-consistent but stale test oracle.
- **Fix:** update the Unreal required paths to `BenchmarkCaptureSessionComponent.h/.cpp` and add a regression test that asserts every engine-required path exists in the real repository tree.
- **Scope:** readiness-audit and regression-test correction only; engine runtime, benchmark measurements, simulation truth, and scoring logic are unchanged.
- **Prevention:** structural readiness tests must validate declared paths against the real checked-in repository layout in addition to isolated synthetic fixtures. Do not let the configuration under test also generate the only source of truth used to verify itself.
- **Verification:** fresh Foundation checks required on the corrective PR head before merge.

## 2026-08-18 — Phase 1 exit gate audit had no CI coverage

- **Observed failure risk:** `prototypes/phase1_exit_gate.py` is the executable contract for the Phase 1 → Phase 2 gate, and `prototypes/test_phase1_exit_gate.py` fully unit-tests it and passed locally, but `Foundation checks` only ran `unittest discover` scoped to each individual prototype subdirectory (`simulation-clock`, `procedural-system`, `coordinate-scale`, `headless-simulation`, `rendering-benchmark`). The top-level `prototypes/test_phase1_exit_gate.py` was never discovered or executed in CI, so a regression in the gate script itself could merge without any independent verification.
- **Root cause:** the workflow was written prototype-by-prototype before the cross-cutting exit-gate audit and its test file were added at `prototypes/` root, and no step was added to cover that location.
- **Fix:** add a `Test Phase 1 exit gate audit` step running `python -m unittest discover -s prototypes -p 'test_*.py' -t prototypes -v`. Confirmed this scope discovers only `test_phase1_exit_gate.py` (the hyphenated prototype subdirectories are not importable Python packages, so `unittest discover` does not descend into them and no test is run twice).
- **Scope:** CI coverage only; `phase1_exit_gate.py` behavior is unchanged.
- **Prevention:** when a new cross-cutting script and test file are added outside the existing per-prototype directories, add an explicit CI discovery step for that location rather than assuming an existing step already covers it.
- **Verification:** fresh GitHub Actions run required on the updated PR head.

## 2026-08-19 — Rendering benchmark scoring weights were untested per-metric

- **Observed failure risk:** `prototypes/rendering-benchmark/benchmark.py:score_result()` is the formula that produces the weighted score behind the Phase 1 engine recommendation (`decision_packet.py` → `PHASE1_EXIT_GATE.md`). Its `DEFAULT_WEIGHTS` dict deliberately assigns different weights per metric (comment: "Weights intentionally favor the things that are hardest to retrofit later"), but every existing test (`test_benchmark.py`, `test_decision_packet.py`, `test_normalization.py`) passed a metrics vector with the same value repeated across all thirteen `REQUIRED_METRICS` (uniform 8.0, or one outlier metric in `test_differentiators_are_sorted_by_weighted_effect`). A uniform vector makes `score_result` collapse to `value * sum(weights)`, which is identical regardless of which weight is paired with which metric name, so no test could detect two weights being swapped between metrics (e.g. a future rebalancing edit or refactor that iterates `REQUIRED_METRICS`/`DEFAULT_WEIGHTS` out of step).
- **Root cause:** all prior scoring tests used degenerate (uniform, or single-outlier) input data that happened to make the per-metric weighting mechanism unobservable, so passing tests gave no real assurance the weight-to-metric wiring was correct.
- **Fix:** added `test_score_applies_each_metrics_own_declared_weight` to `prototypes/rendering-benchmark/test_benchmark.py`, using thirteen distinct per-metric values and asserting `score_result()` against a literal expected weighted sum computed independently of `benchmark.py`'s own iteration.
- **Scope:** test coverage only; `benchmark.py`, `decision_packet.py`, and `normalization.py` behavior unchanged.
- **Prevention:** when a weighted/keyed formula is safety- or decision-relevant, cover it with at least one test whose input values are all distinct so the mapping between keys and their weights/coefficients is actually exercised, not just their sum or count.
- **Verification:** confirmed the new test fails (`4.605 != 5.325`) when `DEFAULT_WEIGHTS["visual_fidelity"]` and `DEFAULT_WEIGHTS["memory_use"]` are deliberately swapped, and that every other test in `test_benchmark.py`, `test_decision_packet.py`, and `test_normalization.py` still passes under that same mutation (proving the pre-existing suite could not have caught it). Reverted the mutation; full `prototypes/rendering-benchmark` suite (113 tests) and the complete Foundation checks command sequence pass clean.

## 2026-08-19 — Rendering benchmark decision-packet deltas were also untested per-metric

- **Observed failure risk:** the entry directly above fixed `benchmark.py:score_result()`'s untested per-metric weighting, but `decision_packet.py:_metric_deltas()` independently re-implements the same `(score delta) * DEFAULT_WEIGHTS[metric]` pattern to build the `top_differentiators`/`all_metric_deltas` evidence in the Phase 1 engine-decision packet. Every existing `test_decision_packet.py` case constructed left/right qualitative assessments that were uniform except for at most one metric (`visual_fidelity`), so every other metric tied (identical left/right score, weighted_delta always 0 regardless of weight). No test could detect `_metric_deltas()` pairing a metric with the wrong `DEFAULT_WEIGHTS` entry.
- **Root cause:** same class as the entry above (degenerate/near-uniform test inputs make a per-key weighting mechanism unobservable), recurring at a second call site that consumes the same weights dict independently rather than through `score_result()`.
- **Fix:** added `test_metric_deltas_apply_each_metrics_own_declared_weight` to `prototypes/rendering-benchmark/test_decision_packet.py`, giving all thirteen `REQUIRED_METRICS` distinct, non-tied left/right deltas (five via distinct quantitative capture values, eight via distinct qualitative assessments) and asserting each row's `weighted_delta` against a value computed with literal weight constants (checked equal to `DEFAULT_WEIGHTS`), independent of `_metric_deltas()`'s own dict lookup and iteration.
- **Scope:** test coverage only; `benchmark.py`, `decision_packet.py`, and `normalization.py` behavior unchanged.
- **Prevention:** when a shared weighted/keyed formula is reused at more than one call site, cover the weighting at each call site independently — a fix for one function's degenerate-input blind spot does not exercise a sibling function that re-reads the same weights dict.
- **Verification:** confirmed the new test fails together with `test_score_applies_each_metrics_own_declared_weight` when `DEFAULT_WEIGHTS["visual_fidelity"]` and `DEFAULT_WEIGHTS["memory_use"]` are deliberately swapped, while all 112 other rendering-benchmark tests still pass under that same mutation. Reverted the mutation; full `prototypes/rendering-benchmark` suite (114 tests) and the complete Foundation checks command sequence pass clean.

## 2026-08-19 — Rendering benchmark capture-field mapping was also untested per-metric

- **Observed failure risk:** `prototypes/rendering-benchmark/normalization.py:normalize_pair()` derives each quantitative benchmark metric from its own named raw-capture field via the `QUANTITATIVE_SOURCES` dict (`cpu_frame_time` -> `cpu_frame_time_ms`, `memory_use` -> `peak_memory_mib`, `build_distribution_complexity` -> `build_size_mib`, etc.) — a third independent call site keyed by metric name, alongside the `DEFAULT_WEIGHTS` wiring already fixed twice above. The existing `test_pair_normalization_derives_objective_scores_and_keeps_audit` test asserted the audited `capture_field` and score for only `cpu_frame_time` and `gpu_frame_time`, and its `memory`/`build` fixture values (900/1800 and 400/800) shared an identical 2:1 ratio, so no existing assertion could have detected `memory_use` and `build_distribution_complexity` being wired to each other's capture fields.
- **Root cause:** same class as the two entries above (a per-key mapping/weighting mechanism exercised only with degenerate or partially-checked inputs), recurring at a third call site that independently keys off metric name.
- **Fix:** added `test_pair_normalization_maps_each_quantitative_metric_to_its_own_capture_field` to `prototypes/rendering-benchmark/test_normalization.py`, giving all five quantitative metrics distinct, pairwise-distinct left/right ratios (2.0, 3.0, 1.5, 1.1, 2.5) and asserting each metric's audited `capture_field` and derived score independently, not just two of the five.
- **Scope:** test coverage only; `normalization.py`, `benchmark.py`, `decision_packet.py`, and other production/simulation behavior unchanged.
- **Prevention:** when a per-key mapping or weighting mechanism is decision-relevant, cover it with distinct, pairwise-distinct input values across every key it handles, not only a subset, and assert both the mapping identity and its downstream effect.
- **Verification:** confirmed the new test fails (`'build_size_mib' != 'peak_memory_mib'`) when `QUANTITATIVE_SOURCES["memory_use"]` and `QUANTITATIVE_SOURCES["build_distribution_complexity"]` are deliberately swapped, while all 114 other rendering-benchmark tests still pass under that same mutation. Reverted the mutation; full `prototypes/rendering-benchmark` suite (115 tests) and the complete Foundation checks command sequence pass clean.

## 2026-08-19 — Run-record capture validation was also untested per-field

- **Observed failure risk:** `prototypes/rendering-benchmark/run_record.py:validate_run_record()` is the gate every raw hardware-capture record must pass before it can reach normalization, scoring, or the decision packet. Its `NUMERIC_NONNEGATIVE_FIELDS` set declares five distinct capture fields (`cpu_frame_time_ms`, `gpu_frame_time_ms`, `peak_memory_mib`, `implementation_hours`, `build_size_mib`) that must each reject a negative or non-numeric value, but `test_run_record.py`'s `test_negative_measurement_is_rejected` exercised only one of the five (`implementation_hours`), and no test exercised non-numeric rejection for any field. A field being silently dropped from `NUMERIC_NONNEGATIVE_FIELDS` (typo, refactor, merge conflict) would let an invalid measurement for that field pass validation unnoticed by any existing test.
- **Root cause:** same class as the three entries above (a per-key validation/mapping mechanism exercised with only one representative key instead of all declared keys), this time in the raw-evidence gate rather than the scoring/weighting layer.
- **Fix:** added `test_every_declared_numeric_field_rejects_a_negative_value` and `test_every_declared_numeric_field_rejects_a_non_numeric_value` to `prototypes/rendering-benchmark/test_run_record.py`, each looping over all five declared fields independently and asserting the exact per-field error message. Added `test_declared_numeric_fields_are_exactly_the_five_expected_capture_fields` to pin the set itself, since the loop tests alone iterate whatever the set currently contains and cannot detect a field being removed from it.
- **Scope:** test coverage only; `run_record.py`, `benchmark.py`, `normalization.py`, `decision_packet.py`, and other production/simulation behavior unchanged.
- **Prevention:** when a per-key validation rule is decision-relevant, cover every declared key independently, and pair the per-key loop test with a test that pins the declared key set itself — a loop alone cannot detect a key silently missing from the set it iterates.
- **Verification:** confirmed the new pinned-set test fails (`'peak_memory_mib'` reported as unexpectedly missing) when `peak_memory_mib` is deliberately removed from `NUMERIC_NONNEGATIVE_FIELDS`, while every other test — including the pre-existing single-field `test_negative_measurement_is_rejected` — still passes under that same mutation (proving the pre-existing suite could not have caught a dropped field). Reverted the mutation; full `prototypes/rendering-benchmark` suite (118 tests) and the complete Foundation checks command sequence pass clean.

## 2026-08-19 — Capture-pair hardware mismatch was also untested per-field

- **Observed failure risk:** `prototypes/rendering-benchmark/capture_pair.py:validate_capture_pair()` is the evidence-integrity gate that rejects a Godot/Unreal capture pair unless they ran on materially identical benchmark hardware. Its `HARDWARE_FIELDS` tuple declares four distinct fields (`os_version`, `cpu_model`, `gpu_model`, `ram_gib`) that must each be compared independently, but `test_capture_pair.py`'s `test_hardware_mismatch_is_rejected` exercised only one of the four (`gpu_model`). A field being silently dropped from `HARDWARE_FIELDS` (typo, refactor, merge conflict) would let a real hardware mismatch for that field pass validation unnoticed by any existing test, letting a non-comparable capture pair feed the Phase 1 engine-decision evidence.
- **Root cause:** same class as the four entries above (a per-key validation mechanism exercised with only one representative key instead of all declared keys), this time in the cross-engine comparability gate rather than the scoring/weighting/normalization/raw-validation layers already covered.
- **Fix:** added `test_every_declared_hardware_field_mismatch_is_rejected` to `prototypes/rendering-benchmark/test_capture_pair.py`, looping over all four declared fields independently (each with a distinct mismatched value) and asserting the exact per-field error message. Added `test_hardware_fields_are_exactly_the_four_expected_fields` to pin the declared set itself, since the loop test alone iterates whatever `HARDWARE_FIELDS` currently contains and cannot detect a field being removed from it.
- **Scope:** test coverage only; `capture_pair.py`, `run_record.py`, `benchmark.py`, `normalization.py`, `decision_packet.py`, and other production/simulation behavior unchanged.
- **Prevention:** when a per-key comparison/validation rule is decision-relevant, cover every declared key independently, and pair the per-key loop test with a test that pins the declared key set itself.
- **Verification:** confirmed both new tests fail (`'ram_gib'` reported as unexpectedly missing from the declared set) when `ram_gib` is deliberately removed from `HARDWARE_FIELDS`, while every other test — including the pre-existing single-field `test_hardware_mismatch_is_rejected` — still passes under that same mutation (proving the pre-existing suite could not have caught a dropped field). Reverted the mutation; full `prototypes/rendering-benchmark` suite (120 tests, up from 118) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Observation-assembly required fields were untested entirely, not just per-field

- **Observed failure risk:** `prototypes/rendering-benchmark/assemble_run_record.py:validate_observation()` gates every engine capture-observation document (measured prefill plus manual-evidence manifest) before it is combined into a run record and validated by `run_record.py`. Its `REQUIRED_OBSERVATION_FIELDS` set declares seven fields (`observation_version`, `engine`, `scenario_name`, `scenario_version`, `captured_at_utc`, `run_record_prefill`, `manual_evidence_still_required`), but no test in `test_assemble_run_record.py` ever removed a field from an observation document — the `missing observation fields: ...` branch had zero coverage, not merely partial per-field coverage like the five sibling gaps already closed elsewhere in this prototype. A field silently dropped from `REQUIRED_OBSERVATION_FIELDS` (typo, refactor, merge conflict) would let an incomplete capture-observation document reach `assemble_run_record()` unnoticed.
- **Root cause:** same class as the five entries above (a per-key validation mechanism with no independent coverage of its declared key set), this time in the observation-assembly layer upstream of `run_record.py` rather than downstream of it.
- **Fix:** added `test_every_declared_observation_field_is_required` to `prototypes/rendering-benchmark/test_assemble_run_record.py`, looping over all seven declared fields independently (each removed one at a time) and asserting the exact per-field error message. Added `test_required_observation_fields_are_exactly_the_seven_expected_fields` to pin the declared set itself, since the loop test alone iterates whatever `REQUIRED_OBSERVATION_FIELDS` currently contains and cannot detect a field being removed from it.
- **Scope:** test coverage only; `assemble_run_record.py`, `run_record.py`, `normalization.py`, `benchmark.py`, `decision_packet.py`, `capture_pair.py`, and other production/simulation behavior unchanged.
- **Prevention:** when a per-key validation rule is decision-relevant, verify it has coverage for at least one key before assuming a "per-field" gap-closing pass is complete — an unexercised branch can hide behind a set that merely looks tested via adjacent tests (e.g. `test_incomplete_evidence_partition_is_rejected`, which exercises a different check inside the same function).
- **Verification:** confirmed the new pinned-set test fails (`manual_evidence_still_required` reported as unexpectedly missing from the declared set) when `manual_evidence_still_required` is deliberately removed from `REQUIRED_OBSERVATION_FIELDS`, while every other test — including the new per-field loop test on the remaining six fields — still passes under that same mutation (proving the pre-existing suite could not have caught a dropped field). Reverted the mutation; full `prototypes/rendering-benchmark` suite (126 tests, up from 124) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Capture-readiness audit had two related zero/partial coverage gaps

- **Observed failure risk:** `prototypes/rendering-benchmark/capture_readiness.py:audit_capture_readiness()` is the first gate in the hardware-capture pipeline: it decides `ready_for_hardware_capture` before any run record, normalization, or scoring happens. Two related gaps existed in `test_capture_readiness.py`. First, `PIPELINE_REQUIRED_FILES` (six pipeline files: `scenario.json`, `scene_manifest.json`, `export_handoff.py`, `prepare_capture.py`, `assemble_run_record.py`, `pipeline.py`) is only ever referenced to *build* the passing test fixture — no test ever removed one of these six files, so the `"missing pipeline file: ..."` blocker branch (lines 59-61) had zero test coverage at all, not merely partial per-field coverage. Second, `ENGINE_REQUIRED_FILES` declares four godot and six unreal source files, but the sole existing missing-source test (`test_missing_engine_source_blocks_capture`) removed only `ENGINE_REQUIRED_FILES["godot"][0]`, so a file silently dropped from either engine's tuple (typo, refactor, merge conflict) could let a genuinely incomplete engine source tree report `source_ready: true` unnoticed, letting hardware capture proceed against an incomplete engine integration.
- **Root cause:** same class as the six entries above (a per-key/per-file validation mechanism exercised with zero or only one representative key instead of all declared keys), this time in the structural-readiness gate upstream of every other rendering-benchmark evidence check.
- **Fix:** added `test_every_declared_pipeline_file_missing_blocks_capture` (loops over all six `PIPELINE_REQUIRED_FILES`, removing one at a time and asserting the exact blocker message) and `test_pipeline_required_files_are_exactly_the_six_expected_files` (pins the declared set) to `test_capture_readiness.py`. Added `test_every_declared_engine_required_file_missing_blocks_capture` (loops over every file in both `ENGINE_REQUIRED_FILES` tuples independently) and `test_engine_required_files_are_exactly_the_expected_declared_files` (pins each engine's *full* file set, not just the two engine keys — a keys-only pin cannot detect a single file dropped from within one engine's tuple, as an initial draft of this fix confirmed).
- **Scope:** test coverage only; `capture_readiness.py` and all other production/simulation behavior unchanged.
- **Prevention:** when pinning a declared key/file set to guard a per-key loop test, pin the full value (e.g. each engine's complete file tuple), not just the outer key set — an outer-keys-only pin can still miss a value silently shrinking.
- **Verification:** confirmed three independent mutations were each caught while leaving every pre-existing test (including the original single-file `test_missing_engine_source_blocks_capture`) passing: (1) removing `scene_manifest.json` from `PIPELINE_REQUIRED_FILES` failed the new pipeline pinned-set test; (2) removing the last unreal file from `ENGINE_REQUIRED_FILES` failed the new per-engine pinned-set test; (3) short-circuiting the unreal branch of the per-engine blocker loop to unconditionally report `source_ready: true` failed six subtests of the new per-file loop test. Reverted each mutation (diffed byte-identical to the pre-mutation file); full `prototypes/rendering-benchmark` suite (130 tests, up from 126) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Scene-state animation-period validation had zero coverage for any declared field

- **Observed failure risk:** `prototypes/rendering-benchmark/scene_state.py:validate_manifest()` rejects a manifest whose `mining_mechanism_period_seconds`, `debris_pulse_period_seconds`, or `asteroid_rotation_period_seconds` animation period is missing or non-positive — the gate that keeps a zero/negative/absent animation period out of the renderer-neutral scene truth every downstream consumer (`export_handoff.py`'s canonical handoff bundle, the Godot and Unreal adapters) treats as authoritative. Unlike the prior entries in this ledger, which each still had partial coverage (typically one representative field or key), `test_scene_state.py` had **no** test at all exercising this branch for **any** of the three fields: no test ever set one to zero, negative, or missing. A field silently dropped from the validation loop (typo, refactor, merge conflict), or the positivity check itself being weakened for one field, would have gone completely undetected.
- **Root cause:** same class as the seven entries above (a per-field/per-key validation mechanism with no independent test coverage of its declared field set), here found with zero representative coverage rather than partial coverage, in the scene-truth contract rather than the evidence/decision-packet pipeline.
- **Fix:** named the previously anonymous inline tuple as a module-level `ANIMATION_PERIOD_FIELDS` constant in `scene_state.py` (matching the existing `NUMERIC_NONNEGATIVE_FIELDS`/`HARDWARE_FIELDS`/`STRING_CAPTURE_FIELDS`/`REQUIRED_OBSERVATION_FIELDS`/`PIPELINE_REQUIRED_FILES` convention) with no change to validation behavior. Added `test_every_declared_animation_period_field_rejects_a_non_positive_value` (loops over all three fields with both a zero and a negative value) and `test_every_declared_animation_period_field_rejects_a_missing_value` (loops over all three fields, deleting each key) to `test_scene_state.py`, plus `test_animation_period_fields_are_exactly_the_three_expected_fields` to pin the declared set itself, since the loop tests alone iterate over the constant and cannot detect a field silently removed from it.
- **Scope:** test coverage only; `scene_state.py` validation behavior, `export_handoff.py`, `playback.py`, and all other production/simulation behavior unchanged.
- **Prevention:** when auditing a prototype for this class of gap, do not assume a validation branch has at least partial coverage merely because the surrounding function is well-tested overall (`test_scene_state.py` had five other passing tests) — check the specific branch directly.
- **Verification:** confirmed two independent mutations were each caught while leaving every pre-existing test passing: (1) removing `asteroid_rotation_period_seconds` from `ANIMATION_PERIOD_FIELDS` failed only the new pinned-set test, while both new per-field loop tests — now only iterating the shrunken constant — still passed, confirming the loop tests alone could not have caught a dropped field; (2) short-circuiting the validation loop to skip `debris_pulse_period_seconds` failed 3 subtests across both new per-field loop tests. Reverted each mutation (diffed byte-identical to the pre-mutation file); full `prototypes/rendering-benchmark` suite (133 tests, up from 130) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Scenario contract's two inline per-field loops were only partially exercised per field

- **Observed failure risk:** `prototypes/rendering-benchmark/scenario.py:validate_scenario()` contains two inline-tuple per-field loops beyond the already-covered `REQUIRED_TOP_LEVEL`/`REQUIRED_CAPTURE_FIELDS` bidirectional checks: one validating `target_fps`, `duration_seconds`, and `simulation_seconds_per_real_second` are each a positive integer, and one validating `camera_sequence`, `required_scene_features`, and `fairness_rules` are each a non-empty list of unique, non-blank strings. `test_scenario.py` exercised only one field from each loop — `simulation_seconds_per_real_second` for the positive-integer check (`test_simulation_time_scale_must_be_positive`) and `fairness_rules` for the duplicate-entry check (`test_duplicate_fairness_rule_is_rejected`), with no coverage at all of the empty-list or blank-entry branches for any of the three list fields. A field silently dropped from either inline tuple (typo, refactor, merge conflict), or the check being weakened for one field, could let a non-positive `target_fps`/`duration_seconds` or an empty/blank/duplicate `camera_sequence`/`required_scene_features` reach the Phase 1 rendering-benchmark scenario contract unnoticed. First confirmed by mutation that the sibling `REQUIRED_TOP_LEVEL`/`REQUIRED_CAPTURE_FIELDS` constants in the same file are *not* affected by this class of gap: `validate_scenario()` checks both of those bidirectionally (missing *and* unexpected) against the literal `scenario.json` fixture, so `test_repository_scenario_is_valid` alone already fails if either constant drifts in either direction from that fixture.
- **Root cause:** same class as the eight entries above (a per-field validation mechanism exercised with only one representative field instead of all declared fields), here in two additional, previously-unaudited loops within the same module whose other two field sets were already covered.
- **Fix:** named the two previously anonymous inline tuples as module-level `POSITIVE_INTEGER_FIELDS` and `UNIQUE_STRING_LIST_FIELDS` constants in `scenario.py` (matching the existing `NUMERIC_NONNEGATIVE_FIELDS`/`HARDWARE_FIELDS`/`STRING_CAPTURE_FIELDS`/`ANIMATION_PERIOD_FIELDS` convention) with no change to validation behavior. Added to `test_scenario.py`: `test_every_declared_positive_integer_field_rejects_zero_and_negative` and `test_every_declared_positive_integer_field_rejects_a_non_integer_value` (looping over all three `POSITIVE_INTEGER_FIELDS`), `test_every_declared_list_field_rejects_an_empty_list`, `test_every_declared_list_field_rejects_a_blank_entry`, and `test_every_declared_list_field_rejects_a_duplicate_entry` (looping over all three `UNIQUE_STRING_LIST_FIELDS`), plus a pinned-set test for each constant, since the loop tests alone iterate over the constant and cannot detect a field silently removed from it.
- **Scope:** test coverage only; `scenario.py` validation behavior and all other production/simulation behavior unchanged.
- **Prevention:** when a module has more than one declared-field set, audit each independently rather than assuming full-set coverage generalizes — this module's `REQUIRED_TOP_LEVEL`/`REQUIRED_CAPTURE_FIELDS` sets were already self-checking via a bidirectional missing/unexpected comparison against a literal fixture, but its two purely-additive per-field loops had no such structural safety net and needed their own coverage.
- **Verification:** confirmed by mutation that `REQUIRED_TOP_LEVEL`/`REQUIRED_CAPTURE_FIELDS` were already covered (removing an entry from either broke `test_repository_scenario_is_valid` immediately) before adding new tests elsewhere. For the actual gap: removing `target_fps` from `POSITIVE_INTEGER_FIELDS` failed only the new pinned-set test while all 139 other tests — including the new per-field loop tests, now only iterating the shrunken tuple — still passed; removing `camera_sequence` from `UNIQUE_STRING_LIST_FIELDS` likewise failed only its own pinned-set test. Reverting each mutation (diffed byte-identical to the pre-mutation file) restored all 140 `prototypes/rendering-benchmark` tests passing (up from 133); full Foundation-checks command sequence (`tools/check_foundation.py` plus all six CI steps, all five prototype suites) passes clean.

## 2026-08-20 — Qualitative-metric completeness checks were exercised on only one metric, and the metric set itself was unpinned

- **Observed failure risk:** `prototypes/rendering-benchmark/pipeline.py:load_qualitative_assessments()` and `finalize_engine_decision.py:parse_qualitative_document()` each independently gate a qualitative-evidence document against `normalization.QUALITATIVE_METRICS` (the eight non-quantitative dimensions of the Phase 1 engine-decision score — `visual_fidelity`, `scene_complexity`, `hud_effort`, `procedural_workflow`, `simulation_integration`, `large_coordinate_behavior`, `save_load_implications`, `commercial_licensing`) before that evidence can reach `build_decision_packet()`. In both `test_pipeline.py` and `test_finalize_engine_decision.py`, the missing-metric rejection test removed exactly one metric via `next(iter(normalization.QUALITATIVE_METRICS))` — whichever metric tuple iteration order happens to yield first (`visual_fidelity`) — rather than looping over all eight, and `QUALITATIVE_METRICS` itself was never asserted as an exact/exhaustive set in either test file. A field silently dropped from the missing-metric detection logic for any of the other seven metrics (typo, refactor, merge conflict), or a metric silently added to or removed from `QUALITATIVE_METRICS` itself, could have gone undetected by either test file.
- **Root cause:** same class as the nine entries above (a per-key validation mechanism exercised with only one representative key instead of all declared keys, and/or an unpinned declared key set), here recurring at two independent call sites (`pipeline.py` and `finalize_engine_decision.py`) that both gate qualitative evidence against the same shared `normalization.QUALITATIVE_METRICS` tuple.
- **Fix:** in `test_pipeline.py`, replaced the single-metric `test_missing_qualitative_metric_is_rejected_before_scoring` body with a loop (via `subTest`) over every metric in `normalization.QUALITATIVE_METRICS`, and added `test_qualitative_metrics_are_exactly_the_eight_expected_metrics` to pin the declared set as a literal expected set. Made the identical pair of changes to `test_finalize_engine_decision.py`'s `test_missing_qualitative_metric_is_rejected`. `QUALITATIVE_METRICS` itself (derived in `normalization.py` from `benchmark.REQUIRED_METRICS` minus `QUANTITATIVE_SOURCES`) was not modified.
- **Scope:** test coverage only; `pipeline.py`, `finalize_engine_decision.py`, `normalization.py`, and all other production/simulation behavior unchanged.
- **Prevention:** when a per-key completeness/validation loop is derived from a shared module-level constant, verify per-key coverage independently at every call site that re-validates against that constant, and pin the constant's exact expected value at each such call site rather than assuming one site's pin covers a sibling site.
- **Verification:** confirmed by two independent mutations, each reverted afterward and diffed byte-identical to the pre-mutation file. (1) Temporarily excluded `"hud_effort"` from `QUALITATIVE_METRICS`' derivation in `normalization.py`: both new pinned-set tests (`test_pipeline.py` and `test_finalize_engine_decision.py`) failed, along with several pre-existing/sibling tests elsewhere in the suite that depend on the full metric set, confirming the pinned-set tests catch a metric silently dropped from the shared constant. Reverted; suite returned to 142/142 passing. (2) Separately, temporarily excluded `"hud_effort"` from the `missing` set computed in `pipeline.py:load_qualitative_assessments()` (leaving all seven other metrics correctly detected) and, independently, from `finalize_engine_decision.py:parse_qualitative_document()`'s equivalent `missing` computation: in each case only the corresponding new per-metric loop test failed — specifically its `hud_effort` subtest — while all 141 other tests, including the same file's own pinned-set test, still passed; the pre-existing single-metric test (which only ever checked `visual_fidelity`) would not have caught either mutation. Reverted both; suite returned to 142/142 passing each time. Full Foundation-checks command sequence (`tools/check_foundation.py` plus all six CI steps, all five prototype suites) passes clean with the real fix in place.

## 2026-08-20 — Capture-pair provenance validation had zero coverage for its per-field checks

- **Observed failure risk:** `prototypes/rendering-benchmark/capture_pair.py:_validate_provenance()` gates each engine's `source_revision`/`handoff_sha256` provenance document before `validate_capture_pair()` will treat a Godot/Unreal capture pair as comparable evidence. Its `REQUIRED_PROVENANCE_FIELDS` set declares two fields, and the function's per-field non-empty-string check (`for field in REQUIRED_PROVENANCE_FIELDS: if not isinstance(value, str) or not value.strip(): raise ValueError(...)`) had **zero** test coverage for either field — no existing test in `test_capture_pair.py` ever supplied a blank or non-string value for `source_revision` or `handoff_sha256`. The same function's missing-field branch (a bidirectional key-equality check shared with the per-field check) also had only partial coverage: `test_incomplete_provenance_is_rejected` exercised only `handoff_sha256` being absent, never `source_revision`. `REQUIRED_PROVENANCE_FIELDS` itself was never pinned as an exact set anywhere in the test file. A field silently dropped from the per-field blank/non-string check (typo, refactor, merge conflict), or a blank/non-string provenance value slipping through, could let a non-comparable or fabricated-looking capture pair reach the Phase 1 engine-decision evidence unnoticed.
- **Root cause:** same class as the nine entries above (a per-key validation mechanism exercised with zero or only one representative key instead of all declared keys, and/or an unpinned declared key set), here in the cross-engine provenance gate rather than the hardware-comparability gate already covered in the same file (`HARDWARE_FIELDS`, entry above).
- **Fix:** added `test_every_declared_provenance_field_is_required` (loops over both fields, removing each independently and asserting the exact `missing: {field}` error), `test_every_declared_provenance_field_rejects_a_blank_value` and `test_every_declared_provenance_field_rejects_a_non_string_value` (loop over both fields with a blank and a non-string value respectively), and `test_required_provenance_fields_are_exactly_the_two_expected_fields` (pins the declared set itself) to `prototypes/rendering-benchmark/test_capture_pair.py`.
- **Scope:** test coverage only; `capture_pair.py` and all other production/simulation behavior unchanged.
- **Prevention:** when a per-key validation mechanism combines a bidirectional key-equality check with a per-key value check on the same declared set, verify both checks independently for every declared key — a single spot-checked field in one branch does not establish coverage of the other branch, nor of the remaining fields in either branch.
- **Verification:** confirmed by three independent mutations, each reverted afterward and diffed byte-identical to the pre-mutation file. (1) Skipping the blank/non-string check specifically for `handoff_sha256` failed only the two new per-field loop tests, each at their `handoff_sha256` subtest, while every pre-existing test still passed. (2) Excluding `source_revision` from the missing-field computation (while the overall bidirectional check still fired) failed only the new `test_every_declared_provenance_field_is_required`, at its `source_revision` subtest, while the pre-existing single-field `test_incomplete_provenance_is_rejected` still passed. (3) Removing `handoff_sha256` from `REQUIRED_PROVENANCE_FIELDS` entirely failed the new pinned-set test along with several pre-existing tests that rely on the same constant's strict key-equality check elsewhere in the function (expected, since this constant also drives that separate check; confirmed the pinned-set test still fails cleanly under this mutation). Reverted each mutation; full `prototypes/rendering-benchmark` suite (146 tests, up from 142) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Evidence-template completeness check had only partial per-field coverage

- **Observed failure risk:** `prototypes/rendering-benchmark/evidence_template.py:missing_evidence_fields()` is the checklist function operators rely on to see what real evidence is still needed before a capture record can reach `run_record.py`'s strict validation boundary. Its per-field loop over `REQUIRED_CAPTURE_FIELDS` (thirteen fields) had three untested branches: the `field not in capture` key-absence branch, the blank/whitespace-only string branch (including the `notes` exemption), and the `capture` object itself failing the `isinstance(capture, Mapping)` check. `test_missing_evidence_is_explicit` only ever exercised the function against the freshly created, all-`None`/empty scaffold, so it incidentally touched the None branch for `cpu_frame_time_ms`, `gpu_frame_time_ms`, `project_settings`, and `screenshots` only — the other nine declared fields' None handling, and every field's absent-key and blank-value handling, had zero coverage. There was also no test proving a fully populated, decision-grade record produces an empty missing list (only that an empty scaffold is reported as incomplete). A field silently dropped from the loop (typo, refactor, merge conflict) could let an operator believe a capture record was decision-ready while a required field was still absent, `None`, or blank.
- **Root cause:** same class as the entries above (a per-key checklist mechanism exercised against only a subset of its declared keys, several of its own branches never independently exercised, and no positive/invariant test proving the "nothing missing" case).
- **Fix:** added `test_every_declared_capture_field_is_flagged_when_absent`, `test_every_declared_capture_field_is_flagged_when_none`, and `test_blank_value_is_flagged_for_every_field_except_notes` (each looping over all thirteen `REQUIRED_CAPTURE_FIELDS`), `test_capture_that_is_not_a_mapping_is_reported_as_missing`, and `test_a_fully_populated_record_has_no_missing_fields` to `test_evidence_template.py`. No production code changed.
- **Scope:** test coverage only; `evidence_template.py` and all other production/simulation behavior unchanged.
- **Prevention:** when a per-key checklist/completeness function has more than one falsifying condition per key (absent, `None`, blank), verify each condition independently across every declared key rather than relying on a single fixture (like the default scaffold) that happens to exercise a few keys under only one of those conditions.
- **Verification:** confirmed by mutation, reverted afterward and diffed byte-identical to the pre-mutation file: skipping the `None` check specifically for `cpu_model` (`if field != "cpu_model" and value is None:`) failed only the new `test_every_declared_capture_field_is_flagged_when_none` test, at its `cpu_model` subtest, while every pre-existing test — including `test_missing_evidence_is_explicit`, which never exercises `cpu_model` — still passed. Reverted; full `prototypes/rendering-benchmark` suite (151 tests, up from 146) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Phase 1 exit gate's per-prototype presence check was exercised for only one of five prototypes

- **Observed failure risk:** `prototypes/phase1_exit_gate.py:audit_phase1_exit()` loops over `PROTOTYPE_REQUIREMENTS` (five Phase 1 prototype areas) and hard-blocks Phase 2 if any prototype directory is absent or empty. `test_phase1_exit_gate.py`'s only negative per-prototype test, `test_missing_prototype_is_a_hard_blocker`, emptied `coordinate_scale` alone; the other four declared prototypes' presence handling, and the "directory does not exist at all" half of the `path.is_dir() and any(path.iterdir())` check for any prototype, had zero coverage. `PROTOTYPE_REQUIREMENTS` itself was never pinned as an exact set. A prototype silently dropped from the loop (typo, refactor, merge conflict) could let Phase 2 be authorized while that prototype area was actually missing or empty.
- **Root cause:** same class as the entries above (a per-key validation mechanism exercised with only one representative key instead of all declared keys, and an unpinned declared key set), here in the Phase 1 exit gate itself rather than one of the individual prototype modules it audits.
- **Fix:** added `test_every_declared_prototype_is_a_hard_blocker_when_empty` and `test_every_declared_prototype_is_a_hard_blocker_when_absent` (each looping over all five `PROTOTYPE_REQUIREMENTS` entries, covering both halves of the presence check) and `test_prototype_requirements_are_exactly_the_five_expected_prototypes` (pins the declared set) to `test_phase1_exit_gate.py`. No production code changed; the hardware rendering-benchmark decision-artifact gate itself is untouched.
- **Scope:** test coverage only; `phase1_exit_gate.py` and all other production/simulation behavior unchanged.
- **Prevention:** when a gate audits a fixed set of declared components, verify every component independently, including all falsifying conditions for "not present" (absent directory vs. empty directory), and pin the declared component set itself.
- **Verification:** confirmed by mutation, reverted afterward and diffed byte-identical to the pre-mutation file: exempting `headless_simulation` from the presence check (`present = (name == "headless_simulation") or (path.is_dir() and any(path.iterdir()))`) failed both new per-prototype loop tests, each at their `headless_simulation` subtest, while every pre-existing test — including `test_missing_prototype_is_a_hard_blocker`, which only ever exercises `coordinate_scale` — still passed. Reverted; the full `prototypes` test-discovery suite (8 tests, up from 5) and the complete Foundation checks command sequence pass clean.

## 2026-08-20 — Coordinate-scale shape contract was untested for two of three axes and for every vector field

- **Observed failure risk:** `prototypes/coordinate-scale/coordinates.py` holds Everward's candidate authoritative position model — the massive-coordinate proof named in `ROADMAP.md` Prototype D and required by `PHASE1_EXIT_GATE.md`. `SpatialPosition.__post_init__` enforces two rules that keep authoritative position exact: every vector must carry exactly three axes, and every `offset_mm` axis must already be normalized into `0 <= value < CELL_SIZE_MM`. `test_coordinates.py` had six tests and exercised the normalization rule with exactly one negative case — `SpatialPosition((0, 0, 0), (CELL_SIZE_MM, 0, 0))` — which places the bad value on the **x** axis only and only at the exclusive upper bound. The y and z axes had zero coverage, as did the below-zero half of the bound on any axis. The three-axis shape rule had **zero** coverage anywhere: no test ever supplied a wrong-length `cell`, a wrong-length `offset_mm`, a wrong-length `xyz_mm` to `from_total_mm()`, or a wrong-length `delta_mm` to `translated_mm()`. `PROOF.md` nevertheless listed "rejection of non-normalized offsets" as a property under test. A weakened or per-axis-exempted check would have let a denormalized offset — the exact condition that makes `total_mm()` ambiguous and breaks exact integer round-tripping — into authoritative simulation state undetected.
- **Root cause:** same class as the eleven entries above (a per-key/per-axis validation mechanism exercised with only one representative key, plus an entirely unexercised sibling branch and an unpinned declared contract). Notably this is the first instance found **outside** `prototypes/rendering-benchmark/`: the preceding eleven closures all audited the engine-decision evidence pipeline, so the four core simulation prototypes had never been audited for this class of gap despite `coordinate-scale` being the thinnest-tested prototype in the repository (6 tests against `rendering-benchmark`'s 151).
- **Fix:** named the previously implicit shape contract as module-level `AXIS_COUNT = 3` and `VECTOR_FIELDS = ("cell", "offset_mm")` constants in `coordinates.py` (matching the existing `NUMERIC_NONNEGATIVE_FIELDS`/`HARDWARE_FIELDS`/`ANIMATION_PERIOD_FIELDS`/`POSITIVE_INTEGER_FIELDS` convention), rewriting `__post_init__`'s two-clause length check as an equivalent loop over `VECTOR_FIELDS` and replacing the bare literal `3`s that mean "number of axes" with `AXIS_COUNT`. Validation behavior, error messages, exception types, and arithmetic are unchanged. Added a `ShapeContractTests` case to `test_coordinates.py` with `test_every_axis_rejects_a_non_normalized_offset` (all three axes × four rejected values: `-1`, `-CELL_SIZE_MM`, `CELL_SIZE_MM`, `CELL_SIZE_MM + 1`), `test_every_axis_accepts_the_largest_normalized_offset` (positive boundary invariant, `CELL_SIZE_MM - 1` on every axis), `test_every_declared_vector_field_rejects_a_wrong_axis_count` (both declared fields × axis counts 0/1/2/4), `test_from_total_mm_rejects_a_wrong_axis_count`, `test_translated_mm_rejects_a_wrong_axis_count`, and the pinned-declared-set tests `test_axis_count_is_exactly_three` and `test_vector_fields_are_exactly_the_two_expected_fields`, since the loop tests alone iterate the constants and cannot detect an axis or field silently removed from them.
- **Scope:** test coverage plus behavior-preserving constant naming only; the position model's arithmetic, normalization, rebasing, and render-conversion behavior are unchanged, and no other prototype or production module was touched.
- **Prevention:** this class of audit had been applied only to `prototypes/rendering-benchmark/`. Apply it to the core simulation prototypes as well (`simulation-clock`, `procedural-system`, `headless-simulation` remain unaudited for it) — a prototype being small and long-green is not evidence its validation branches are covered.
- **Verification:** confirmed by four independent mutations, each reverted afterward and diffed byte-identical to the pre-mutation file, and in every case the six pre-existing `SpatialPositionTests` all still passed. (1) Exempting axis index 1 from the normalization check (`... for i, value in enumerate(self.offset_mm) if i != 1`) failed only `test_every_axis_rejects_a_non_normalized_offset`, at its four `axis=1` subtests. (2) Replacing the `VECTOR_FIELDS` loop with `if len(self.cell) != AXIS_COUNT:` (dropping `offset_mm` from the shape check) failed only `test_every_declared_vector_field_rejects_a_wrong_axis_count`, at its four `field='offset_mm'` subtests. (3) Shrinking `VECTOR_FIELDS` to `("cell",)` failed **only** the pinned-set test — the per-field loop test passed, because it iterates the shrunken tuple — confirming the pinned-set test is the necessary detector for a silently dropped field. (4) Deleting both the `from_total_mm()` and `translated_mm()` axis-count checks failed only the two new entry-point tests (2 failures, 6 errors). Reverted all mutations; `prototypes/coordinate-scale` now runs 13 tests (up from 6) and the complete Foundation checks command sequence (`tools/check_foundation.py` plus all six CI steps) passes clean at 213 tests total, up from 206.
