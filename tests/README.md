# Tests

Cross-cutting automated tests and test fixtures will live here once executable prototypes begin.

The testing priority is deterministic, headless simulation behavior. See `docs/TESTING_STRATEGY.md`.

Expected categories include unit tests, simulation integration tests, determinism regressions, golden-seed fixtures, save-migration fixtures, invariants, and performance/stress scenarios.
