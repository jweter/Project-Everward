# Source

Production source code will live here after the Phase 1 technical proofs establish the engine and runtime architecture.

Do not prematurely turn a prototype into production structure. Code promoted from `prototypes/` should first satisfy the relevant acceptance criteria and architecture decision.

The eventual source layout should preserve the separation between authoritative simulation, application/orchestration, presentation, and persistence described in `docs/ARCHITECTURE.md`.
